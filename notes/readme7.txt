Syntax is basically the grammar of something like the format.

An example of syntax is for syntax.

The Syntax is :

for( ; ; ){

}


To define your own data type, you can put class followed by your very own data type name.

A class is can be refered to as a cookie cutter or a template. We create objects with classes.

An action is something that can be completed in Java. for example: run for three miles, eat food.


